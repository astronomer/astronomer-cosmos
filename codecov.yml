ignore:
  - "cosmos/airflow/_override.py"

coverage:
  status:
    patch:
      default:
        target: auto
        threshold: 2%
        only_pulls: true
        if_ci_failed: success
        removed_code_behavior: fully_covered_patch
    project:
      default:
        target: auto
        threshold: 0.5%
