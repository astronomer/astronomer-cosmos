manifests:
  - name: project_b
    type: file
    config:
      # Use environment variable for flexibility, with fallback to relative path
      # path: '{{ env_var("PROJECT_B_MANIFEST_PATH", "../project_b_bidirectional/target/manifest.json") }}'

      # For GitHub Actions Integration Tests CI run, set the path to the manifest.json file
      path: /home/runner/work/astronomer-cosmos/astronomer-cosmos/dev/dags/dbt/cross_project/project_b_bidirectional/target/manifest.json
enable_telemetry: false
