manifests:
  - name: project_a
    type: file
    config:
      # Use environment variable for flexibility, with fallback to relative path.
      # CI exports PROJECT_A_MANIFEST_PATH (see scripts/test/integration-dbt-loom.sh); locally
      # the relative fallback resolves against the project directory during dbt parse.
      path: '{{ env_var("PROJECT_A_MANIFEST_PATH", "../project_a_bidirectional/target/manifest.json") }}'
enable_telemetry: false
